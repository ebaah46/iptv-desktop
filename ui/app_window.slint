import { HomeScreen } from "screens/home_screen.slint";
import { HomeState } from "globals/home_state.slint";

export { HomeState }
export component MainWindow inherits Window {
    width: 1024px;
    height: 720px;
    title: "IPTV Desktop";

    background: #13131a;

    HomeScreen {
        width: 100%;
        height: 100%;
    }
}